// swift-tools-version: 5.6

// WARNING:
// This file is automatically generated.
// Do not edit it by hand because the contents will be replaced.

import PackageDescription
import AppleProductTypes

let package = Package(
    name: "Virgil Pod",
    platforms: [
        .iOS("15.2")
    ],
    products: [
        .iOSApplication(
            name: "Virgil Pod",
            targets: ["AppModule"],
            bundleIdentifier: "com.heykwarb.Virgil-Pod",
            teamIdentifier: "VVD3L3V6TF",
            displayVersion: "1.0",
            bundleVersion: "1",
            appIcon: .placeholder(icon: .magicWand),
            accentColor: .presetColor(.yellow),
            supportedDeviceFamilies: [
                .pad,
                .phone
            ],
            supportedInterfaceOrientations: [
                .portrait,
                .landscapeRight,
                .landscapeLeft,
                .portraitUpsideDown(.when(deviceFamilies: [.pad]))
            ],
            capabilities: [
                .mediaLibrary(purposeString: "access Music Library for playing")
            ]
        )
    ],
    targets: [
        .executableTarget(
            name: "AppModule",
            path: "."
        )
    ]
)